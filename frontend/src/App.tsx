import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import Base from './routes'

function App() {
  return (
    <BrowserRouter>
        <Base />
    </BrowserRouter>
  );
}

export default App;
